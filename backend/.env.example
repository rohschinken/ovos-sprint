NODE_ENV=development
PORT=3001
DATABASE_URL=./data/ovos-sprint.db
JWT_SECRET=your-secret-key-change-in-production
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001

# Email Configuration
# -------------------
# Development (Mailpit - no authentication required)
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_FROM_EMAIL=noreply@ovos-sprint.local
SMTP_FROM_NAME=OVOS Sprint

# Production (SparkPost SMTP Relay)
# SMTP_HOST=smtp.sparkpostmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=SMTP_Injection
# SMTP_PASSWORD=your-sparkpost-api-key
# SMTP_FROM_EMAIL=noreply@yourdomain.com
# SMTP_FROM_NAME=OVOS Sprint

# Database Seeding (optional - for non-interactive deployment)
# ADMIN_EMAIL=af@ovos.at
# ADMIN_PASSWORD=your-secure-password
